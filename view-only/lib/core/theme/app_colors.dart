import 'package:flutter/material.dart';

/// AQUA-app proximate tokens (DANONE view-only file blocks plugin eval;
/// values taken from the environment's shared variable collections).
/// See REPORT.md for the extraction blocker and reproduction steps.
abstract final class AquaColors {
  static const sand = Color(0xFFF6F2EA);
  static const surface = Color(0xFFFFFFFF);
  static const mint = Color(0xFFD7EADF);
  static const aqua = Color(0xFF79C7C9);
  static const forest = Color(0xFF1E4D3C);
  static const ink = Color(0xFF22332C);
  static const muted = Color(0xFF58625B);
  static const coral = Color(0xFFF2764F);
}
